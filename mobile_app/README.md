# Mobile Responder UI (demo)

A minimal single‑page HTML/JS app that connects to the orchestrator via
Server‑Sent Events (SSE) and displays incoming incident alerts.

Run the app with `python -m http.server 8000` inside the `mobile_app` folder and open `http://<host>:8000` on a phone.
